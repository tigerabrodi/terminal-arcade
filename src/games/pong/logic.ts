// TODO: Implement with TDD
// Pure functions - no OpenTUI imports allowed in this file
//
// Functions to implement:
// - tick(args: { state, input }) -> PongState
// - movePlayer(args: { paddle, direction, speed, gameArea }) -> Paddle
// - moveCpu(args: { paddle, ball, speed, gameArea }) -> Paddle
// - moveBall(args: { ball, speed }) -> Ball
// - checkWallBounce(args: { ball, gameArea }) -> Ball
// - checkPaddleCollision(args: { ball, player, cpu, gameArea }) -> Ball
// - checkScore(args: { ball, gameArea }) -> { playerScored, cpuScored }
// - resetBall(args: { gameArea, direction }) -> Ball

export {}
