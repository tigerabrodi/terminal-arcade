// TODO: Implement with TDD
// Pure functions - no OpenTUI imports allowed in this file
//
// Functions to implement:
// - tick(args: { state, input }) -> DinoState
// - applyJump(args: { player, jumpStrength }) -> Player
// - applyGravity(args: { player, gravity, groundY }) -> Player
// - applyDuck(args: { player, isDucking }) -> Player
// - moveObstacles(args: { obstacles, speed }) -> Array<Obstacle>
// - spawnObstacle(args: { gameArea, groundY }) -> Obstacle
// - checkCollision(args: { player, obstacles }) -> boolean
// - calculateSpeed(args: { score, baseSpeed }) -> number

export {}
