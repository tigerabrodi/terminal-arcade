// TODO: Write tests first (TDD)
//
// Test cases to cover:
// - Snake moves in current direction each tick
// - Snake cannot reverse direction (e.g. right -> left)
// - Snake grows when eating food
// - Score increments when eating food
// - Game over when hitting wall
// - Game over when hitting self
// - Food spawns in unoccupied cell
// - New food spawns after eating
// - Tick interval decreases as score increases

import { describe, test, expect } from 'bun:test'

describe('snake logic', () => {
  test.skip('snake moves in current direction', () => {
    expect(true).toBe(true)
  })
})
