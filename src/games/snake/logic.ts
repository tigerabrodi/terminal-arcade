// TODO: Implement with TDD
// Pure functions - no OpenTUI imports allowed in this file
//
// Functions to implement:
// - tick(args: { state, input }) -> SnakeState
// - spawnFood(args: { state }) -> Position
// - checkCollision(args: { state }) -> boolean
// - getNextHead(args: { state }) -> Position
// - isOppositeDirection(args: { current, next }) -> boolean

export {}
