// TODO: Implement with TDD
// Pure functions - no OpenTUI imports allowed in this file
//
// Functions to implement:
// - tick(args: { state, input }) -> FlappyBirdState
// - applyGravity(args: { bird, gravity }) -> Bird
// - flap(args: { bird, flapStrength }) -> Bird
// - movePipes(args: { pipes, speed }) -> Array<Pipe>
// - spawnPipe(args: { gameArea, tickCount }) -> Pipe
// - checkCollision(args: { bird, pipes, gameArea }) -> boolean
// - updateScore(args: { bird, pipes }) -> { pipes, scoreGained }

export {}
