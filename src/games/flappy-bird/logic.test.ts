// TODO: Write tests first (TDD)
//
// Test cases to cover:
// - Bird falls with gravity each tick
// - Flap sets upward velocity
// - Pipes move left each tick
// - New pipe spawns at correct interval
// - Pipe gap is within valid range
// - Collision detected when bird hits pipe
// - Collision detected when bird hits floor/ceiling
// - Score increments when bird passes pipe
// - Pipes removed when off screen

import { describe, test, expect } from 'bun:test'

describe('flappy bird logic', () => {
  test.skip('bird falls with gravity each tick', () => {
    expect(true).toBe(true)
  })
})
