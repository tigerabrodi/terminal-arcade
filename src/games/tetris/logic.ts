// TODO: Implement with TDD
// Pure functions - no OpenTUI imports allowed in this file
//
// Functions to implement:
// - tick(args: { state, input, randomValue }) -> TetrisState
// - movePiece(args: { piece, dx, dy, board }) -> Piece | null
// - rotatePiece(args: { piece, board }) -> Piece | null
// - dropPiece(args: { piece, board }) -> Piece
// - lockPiece(args: { piece, board }) -> Array<Array<Cell>>
// - clearLines(args: { board }) -> { board, linesCleared }
// - calculateScore(args: { linesCleared, level }) -> number
// - checkGameOver(args: { piece, board }) -> boolean
// - isValidPosition(args: { piece, board }) -> boolean
// - getRandomPiece(args: { randomValue }) -> PieceKind

export {}
